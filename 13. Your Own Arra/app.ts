// Store examples of your favorite mode of transportation in an array
let transportationExamples: string[] = ["Honda motorcycle", "Civic car", "BMX bicycle", "Scooty"];

// Print statements about each item in the list
for (let i = 0; i < transportationExamples.length; i++) {
    console.log(`I would like to own a ${transportationExamples[i]}.`);
}
